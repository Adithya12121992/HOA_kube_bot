apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: producer-consumer-pvc
  namespace: hoa-pipeline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi
  storageClassName: local-path
