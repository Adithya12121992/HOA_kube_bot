# Phase 1: Core Services
# Web Framework & API
fastapi==0.100.1
uvicorn==0.23.2
pydantic==2.9.2
python-multipart==0.0.6

# Messaging
pika==1.3.2

# Web Framework (legacy, for backward compatibility)
Flask==3.0.0
Werkzeug==3.0.1
python-dateutil==2.8.2

# Utilities
requests==2.31.0
python-dotenv==1.0.0

# Phase 2: RAG Pipeline (extraction, cleaning, chunking, embedding, storage)
pdfplumber==0.10.3
sentence-transformers==2.7.0
chromadb==0.4.22
numpy==1.26.4
# cryptography (transitive, via pdfplumber -> pdfminer.six) is pinned
# explicitly: 50.0.0 (the unpinned default at time of writing) crashes with
# SIGILL (exit 132) on `from cryptography.hazmat.primitives.ciphers import
# ...` under this Docker Desktop arm64 virtualization - confirmed via direct
# reproduction, and confirmed 42.0.5 doesn't have the issue.
cryptography==42.0.5

# Phase 3: Cloud environment bundle (Pinecone storage)
# pinecone pinned to <7.0.0 here (down from the 9.1.0 this project started
# with) because llama-index-vector-stores-pinecone 0.4.5 requires
# pinecone>=3.2.2,<7.0.0. Re-verified add_chunks()/search()/reset() against
# the real index after the downgrade - no behavior change, same API surface
# this project actually uses (Pinecone(api_key=...), pc.Index(name),
# index.upsert/query/delete).
pinecone==6.0.2

# Phase 5: Conversation memory (cloud environment bundle)
mem0ai==2.0.18

# Phase 5: RAG framework, cloud bundle (real LlamaIndex retrieval over the
# existing Pinecone index, replacing the direct-SDK-only cloud search path)
llama-index-core==0.12.20
llama-index-vector-stores-pinecone==0.4.5
